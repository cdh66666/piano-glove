# 六自由度钢琴手套 · Piano Glove

用 6 个舵机驱动的可穿戴手套：通过连杆/绳驱带动佩戴者的手指按压琴键。
**上层玩法是导入 MIDI 文件，让手套按原曲的节奏和速度带动你的手指** —— 戴上去感受大师级的演奏。

> **状态（2026-09-18 晚）**
> - **固件自研完成**（`PIANO_GLOVE_2` v2.1.2）：稳态吞吐 ≈ **1421 条/秒**，比原厂快两个数量级
> - **6 块舵机（`id=1..6`）全部在线**，实机逐根验证 `MOVE` 真的产生位移（271~592 计数，`servo_err` 不涨）
> - **按压幅度 = 完整校准行程**：修掉「静止位取量程中点 → 只按半个行程」，实机 6 槽全部达标
> - **消除撞限位**：`MOVE` 加校准区间越界闸；界面不再硬编码 `MOVE 1 200`（它本来对 6 槽全部越界）
> - **速度已实测达标**：满行程 **4.97 Hz**（保底 4 Hz ✓），行程压到 60% → **7.18 Hz**（6 Hz ✓）
> - **音乐 ↔ 演奏动作同轴显示**：演奏时浏览器现场合成声音；卷帘图上半张画曲子（音高 × 时间）、
>   下半张逐根画参与演奏的手指的动作，共用一根播放头 —— **听到的 = 看到的 = 手套做的**
> - **演奏速度分级**：曲库每首自带本级建议速度（L1 90% → L5 200%，选中即套用），滑块上限 250%；
>   旁边实时算出「当前速度相当于手套上限的几倍」—— **极限在哪是看得见的数字**。
>   L5 故意超上限，用来压测固件的丢音降级
> - **声音可自证**：演奏页有「🔔 试听一下」和一行音频状态 ——
>   再遇到"没声音"时，一眼就能分清是页面问题还是输出环境问题
> - **串口不走浏览器**：`host/bridge.py` 在本机进程里用 pyserial 开串口，页面只发 HTTP。
>   于是**侧边栏浏览器 / 内嵌 WebView 也能连板子**（它们没有 `navigator.serial`，原来必然连不上）。
>   串口**自动选、自动连**：按芯片型号打分 → 逐个问 `INFO` → 谁答连谁，拔了再插自己回来；
>   界面上没有"选模式"这一步，也没有模拟模式
> - 调试台分页式；**页面端到端自测 199 项 + 串口桥自测 43 项全绿**（脚本自报总数，
>   桥那几条新断言全部做过变异测试 —— 往代码里注入退化，确认对应的断言真的会红），
>   MIDI 解析器经独立实现交叉验证
> - **实机复验通过（2026-09-18 用户实测）**：试动作满幅度、不再撞限位；完整曲目演奏节奏正确
> - 演奏样本自带**《两只老虎》**，另有 **10 首分级曲库**（L1 入门 → L5 挑战）可在演奏页下拉直选；
>   样本**不含指法**，手指分配由程序现场算，指法表见 [`host/samples/FINGERING.md`](host/samples/FINGERING.md)

---

## 硬件

| 项目 | 规格 |
| --- | --- |
| 主控板 | 微雪 **Servo Driver with ESP32**（SKU 21593）：ESP32 + CP2102 + OLED + WiFi + DC 6~12V 供电 |
| 主控固件 | **自研 `PIANO_GLOVE_2`**（本仓库 `firmware/`，源码在手）。原厂闭源 `PIANO_GLOVE_1` 已备份，无串口透传 |
| 舵机 | 6 × **Feetech STS3032-C001**：12bit 磁编码，量程 0~4095（360°），工作电压 4.8~6V，TTL 半双工 |
| PC ↔ 板 | USB-C（UART0），**固定 115200 8N1** |
| 板 ↔ 舵机 | UART1，**GPIO18=RX / GPIO19=TX，1,000,000 bps** |
| 供电 | **5V/5A 适配器接 DC 圆口**（USB-C 只给板子供电，不带舵机） |
| 舵机数 / 自由度 | 6 个舵机，但**能独立按琴键的只有 5 根**：拇指 2 个舵机是「1 根按压 + 1 根侧摆摆位」，其余四指各 1 |

> ⚠️ 方向别搞混：这只手套是**被 MIDI 驱动去动手指**的，不是把手指动作转成 MIDI 的输入型设备。

### 槽位 ↔ 舵机（固件默认）

| slot | 名称 | 角色 | 参与演奏 | 默认舵机 ID |
| --- | --- | --- | --- | --- |
| 0 | 拇指按压 | 把拇指压到琴键上 | ✔ | 1 |
| 1 | 拇指侧摆 | 演奏前把拇指摆到能压到键的位置（使能位） | ✘ | 2 |
| 2 | 食指 | 按压 | ✔ | 3 |
| 3 | 中指 | 按压 | ✔ | 4 |
| 4 | 无名指 | 按压 | ✔ | 5 |
| 5 | 小指 | 按压 | ✔ | 6 |

> ⚠️ **ID 映射已实机核对**（槽位 0~5 → 舵机 1~6，`[1,2,3,4,5,6]`）。
> **拇指两轴的命名改过**（2026-09-20）：实机确认出厂标签是反的 ——
> 真的"按下去"是 slot0，真的"往旁边摆"是 slot1。
> 哪根是哪根在调试台第 4 步能一键对调（只换角色，不动 ID、不作废校准）。
> 如果某根长指的按压感觉反了，用 `SETDIR` 单独修正即可，**不用重做编号**。
> 详见 `docs/design-notes.md` §3.5。

---

## 六个最容易踩的坑

1. **PC 侧别直接发原始 Feetech SCS 包。** 板子对外是**文本命令协议**，没有串口透传；舵机原始包发过去毫无反应。
   文本命令表见 [docs/handoff-2026-09-17.md](docs/handoff-2026-09-17.md#2-固件-piano_glove_1-命令协议核心)。
2. **6 个舵机出厂 ID 全是 1，不是 1~6。** 同时挂上总线会互相抢答。**改号必须一次只接一个舵机**；
   而且编号向导第 N 轮插上来的新舵机**也是 1** —— 所以「改号」和「给这个槽位登记 id」是两件事，
   混为一谈会把上一轮编好的槽位一起改掉。详见 [docs/design-notes.md](docs/design-notes.md) 风险 3。
3. **校准是按「槽位」存的，不是按舵机 ID 存的。** 一旦动过 `MAP` / `SETID`，旧校准立即作废，
   **必须重做第 3 步校准**。拿错校准去按压可能直接顶到机械限位。
   （例外：若偏差只是**静止位取错端点**造成的，`snapStandby()` / `CAL ALIGN` 会自动修好，
   不用重做采样 —— 2026-09-18 的 6 个槽位就属于这种，现已全部达标。）
   `host/tests/bench_move.py` 内置了这道安全闸（校准确与当前位置差超过 600 计数就拒绝动作）。
   想先体检不重做，用 `host/tests/diag_cal.py` 把 6 槽的 `min/standby/max/方向/当前位置` 全列出来。
4. **改完固件一定要真烧录并回读 `INFO ver=` 复核。** 「本地编译通过」不等于「板子上跑的是这版」——
   曾经因此出现过「试动作点了只锁电机不动」：板子上还是旧版本，新代码从没烧进去。
5. **`standby`（静止位）必须落在「松开端」，不是量程中点。** 取中点的话
   `pressPos(s,100)` 只从**中点**走到按下端，按压幅度只剩你校准行程的**一半** ——
   表现就是「怎么幅度这么小」。v2.1.2 已修，并对存量校准加了开机自动对齐（`snapStandby()` / `CAL ALIGN`）。
6. **`MOVE <id> <pos>` 收的是原始寄存器位置，不要在界面上硬编码。** 曾经写死 `MOVE 1 200`，
   而 `200` 对 6 块舵机**全部**落在校准区间外，舵机一路顶到限位卡死。
   现在固件有越界闸（要越界得显式带 `FORCE`），界面则改用 `PRESS`/`RELEASE` 走校准区间。

---

## 仓库结构

```
piano-glove/
├── docs/
│   ├── handoff-2026-09-17.md   ★ 交接文档（协议逆向、踩坑、进度、路线的唯一事实来源）
│   └── design-notes.md         决策记录 · 待定问题 · 里程碑
├── host/                       PC 侧上位机与调试工具
│   ├── start-debug.bat         ★ 一键启动：起串口桥 + 打开调试台（缺 pyserial 会自动装）
│   ├── bridge.py               ★ 本地串口桥：pyserial 开串口 + 托管页面 + HTTP API
│   │                             自动扫描/打分/连接、热插拔重连、跨站请求拦截
│   ├── web_piano_glove.html    ★ 分页式调试台（底部导航 6 页，含 MIDI 演奏）
│   ├── glove_fw.py             PIANO_GLOVE 协议 Python 封装 + CLI
│   ├── assign_ids.py           舵机 ID 编号向导（命令行，支持断电续跑）
│   ├── live.html               编号向导实时进度网页
│   ├── assign_log.txt          编号运行日志（协议逆向的第一手证据）
│   ├── samples/                ★ 演奏样本
│   │   ├── twinkle.mid …       10 首分级样本（L1 入门 → L5 挑战），**不含指法信息**
│   │   ├── FINGERING.md        自动指法表（跑页面里那份 buildPlan 导出）
│   │   └── two-tigers.mid      《两只老虎》，调试台内置示例用
│   ├── tests/                  ★ 自测与实机验证
│   │   ├── e2e.js              页面端到端自测（199 项）
│   │   ├── bridge_check.py     串口桥自测（43 项，真进程 + 假板子）
│   │   ├── fake_glove.py       假手套：TCP 说手套协议，供 pyserial 的 socket:// 接
│   │   ├── mutate_bridge.js    变异测试：往桥那几条路上注入退化，确认断言会红
│   │   ├── mutate_play.js      变异测试：演奏时序 10 处退化，逐条确认断言会红
│   │   ├── diag_timing.js      ★ 演奏时序诊断：按住时长/同手指间隔/行程扫描
│   │   ├── make_samples.py     生成 10 首分级样本 + 内嵌进调试台曲库
│   │   ├── fingering_report.js 导出自动指法表
│   │   ├── bench_move.py       ★ 实机主力：验位移 + 测速
│   │   └── diag_state.py       读板子真实状态
│   ├── scservo_sdk/            飞特官方 Python SDK（原始 SCS/STS 协议用）
│   └── legacy/                 ⚠️ 与当前固件不兼容，仅作 UI 素材参考
├── firmware/                   ★ 自研固件 PIANO_GLOVE_2 源码（PlatformIO）
│   ├── src/                    main.cpp / glove.cpp / scs_bus.cpp
│   └── README.md               ★ 协议细节、寄存器表、bug 剖析、实机速度基准
└── hardware/                   结构件与电路
```

---

## 快速开始

### 网页调试台

**Windows 一键启动**：双击 `host/start-debug.bat`

它会起一个**本地串口桥**（`host/bridge.py`，127.0.0.1:8123）并打开调试台。串口由这个 Python 进程用 pyserial 直接打开，**页面只发 HTTP**。

> **为什么要这样绕一下**：网页想开串口只有 Web Serial（`navigator.serial`）这一条路，而它
> - 只有电脑版 Chrome / Edge 有；
> - 要求安全上下文，`file://` 打开直接没有；
> - **侧边栏浏览器、内嵌 WebView、微信/豆包内置浏览器一律没有**。
>
> 结果就是「在侧边栏里点连接永远没反应」—— 那不是驱动问题，是那个浏览器没有这个 API。
> 把串口挪到 Python 侧之后，**任何能打开网页的浏览器都能连板子**。

手动方式：

```bash
cd host
python bridge.py                 # 起桥（默认 127.0.0.1:8123，自动扫描 + 自动连接）
python bridge.py --port 8123 --url COM4    # 想写死某个口也行
```

然后浏览器打开 <http://127.0.0.1:8123/web_piano_glove.html>。

**不用选串口、不用点连接**：桥会枚举本机串口 → 按芯片型号打分 → 从高到低逐个打开、问一句 `INFO` → 谁回 `OK INFO` 就连谁。板子拔了再插会自己连回来。界面上没有「模拟模式」这种选项，只有一条真串口的路。

> 打分规则：CP210x（微雪板载，100）> CH340（92）> ESP32-S3 原生 USB（88）> FTDI（84）> 认不出芯片的 USB 口（20）。
> 蓝牙虚拟口、主板自带 ACPI 通信口、并口直接拉黑，不去浪费那一次开关串口（**开串口会让 ESP32 复位**，多开一次就多等一轮启动）。
>
> 波特率固定 **115200**。

### 无硬件自测

```bash
cd host/tests
python make_midi.py && python ref_parse.py && node compare.js   # MIDI 解析器交叉验证
node e2e.js                            # 页面端到端，199 项检查
python bridge_check.py                 # 串口桥自测，43 项（真进程 + 假板子）
node mutate_bridge.js                  # 往这两条路上注入退化，确认断言真的会红
```

`e2e.js` 需要带 `?sim=1` 打开页面：这是**留给自测的后门**，用页面内置的假固件顶替串口，界面上没有任何入口。第 8 节反过来验证「不带 `?sim=1` 时页面上根本没有模拟模式、并且打开就自动连上串口」。

`bridge_check.py` 的假东西只有"对面那块板子"（`tests/fake_glove.py` 用 TCP 说手套协议，pyserial 用 `socket://` 接上去），bridge、HTTP、收发线程全是真的。

### 实机验证（插上板子和舵机）

```bash
cd host/tests
python bench_move.py              # 验「舵机真的动了」：发指令前后各读一次位置，比差值
python bench_move.py --slot 1,2,3 # 只测某几根
python diag_state.py              # 读板子落在 NVS 里的真实状态（编号 / 校准 / 总线诊断）
```

详见 [host/tests/README.md](host/tests/README.md)。

### Python 命令行

```bash
cd host
pip install -r requirements.txt

python glove_fw.py info                    # 固件 / 档位 / 校准 / armed 状态
python glove_fw.py assign                  # 逐个把舵机 ID 编成 1~6
python glove_fw.py auto                    # 固件自检（含诊断建议字段）
python glove_fw.py status                  # 6 槽位总表
python glove_fw.py cal wizard 20           # 一键校准向导（20 秒活动时间）
python glove_fw.py monitor                 # 轮询实时反馈
python glove_fw.py raw "CAL STATUS"        # 发任意命令
```

板子串口**自动识别**：`glove_fw.py` 会挨个串口发 `INFO`，回 `OK INFO` 的那个就是手套。串口号随 USB 枚举变化，别写死；要指定就 `--port COMx`。

---

## 当前进度

| 项目 | 状态 |
| --- | --- |
| 固件协议逆向 | ✅ 完成 |
| 舵机档位设为 `STS3032` | ✅ 完成 |
| 调试台上位机（分页式） | ✅ 完成，串口由本机 bridge 代开，任意浏览器可用 |
| MIDI 解析 / 分配 / 演奏引擎 | ✅ 完成，自测通过 |
| **本地串口桥**（`host/bridge.py`） | ✅ 自动选推荐口 + 自动连接 + 热插拔重连 |
| 页面端到端自测（`host/tests/e2e.js`） | ✅ **199 项全绿**，解析器交叉验证 0 差异 |
| 串口桥自测（`host/tests/bridge_check.py`） | ✅ **43 项全绿**（真桥进程 + 假板子） |
| 桥那条路的变异测试（`mutate_bridge.js`） | ✅ 3 处注入退化 → 5 条断言变红 |
| **自研固件 `PIANO_GLOVE_2`** | ✅ v2.1.2 已烧录，编译 RAM 6.7% / Flash 22.2% |
| **固件吞吐** | ✅ 稳态 ≈ **1421 条/秒**（原厂 9.9 条/秒） |
| 舵机编号 1~6 | ✅ 实机映射已修正为 `[1,2,3,4,5,6]`，`SCAN` 稳定报 6 块 |
| 6 槽位在线 | ✅ 全部 `online=1` |
| **`MOVE` 真的让舵机动** | ✅ 实机逐根验证：**6 根位移 271~592 计数**，`servo_err` 不涨 |
| **速度实测（`RATE`）** | ✅ 满行程 **4.97 Hz**；行程 60% → **7.18 Hz**。见下表 |
| 演奏样本 | ✅ 《两只老虎》+ **10 首分级曲库**（L1~L5），全部不含指法信息 |
| **自动指法** | ✅ 手指分配由 `buildPlan()` 现算；指法表 `host/samples/FINGERING.md` 由真实浏览器跑同一份算法导出 |
| **演奏速度分级** | ✅ L1 90% → L5 200%（曲库下拉直选即套用），滑块上限 250%，含「⚡ 拉满」 |
| **演奏中的声音** | ✅ Web Audio 现场合成，与动作走**同一条事件流**；含「🔔 试听」+ 音频状态自检 |
| **起音点 / 音画对齐** | ✅ 声音落在**手指行程的一半**（默认 50%，可调 0~90%）—— 跟真钢琴一样压到中段就击弦；提交时刻 = 按下 + 行程×起音点 − 声卡延迟，默认净提前 15ms。演奏页有两行控件，卷帘图动作条上画起音刻度 |
| **音高对应的音域分配** | ✅ 修掉「窄音域只占满 4 根手指」的 bug（《小星星》6 个音现各用一根），有回归断言 |
| **音乐 ↔ 演奏动作同轴显示** | ✅ 卷帘（音高×时间、按手指着色）+ **5 条动作轨**（侧摆那根没有轨，它整场不动）+ 播放头；Web Audio 现场合成，默认只响手套真的按下的音，丢音画灰影 |
| **按压幅度 = 完整校准行程** | ✅ 实机 6 槽全部达标（静止位在松开端，行程 = 量程 702/692/504/586/815/840 计数） |
| **`MOVE` 越界闸** | ✅ 实机确认旧硬编码 `MOVE 1 200` 对 6 槽全部越界，闸生效后拒收 |
| 6 槽位校准 | ✅ 实机读回 6 槽 `valid=1`，每块舵机当前位置都在自己的校准区间内 |
| ARM / 按压 / DEMO | ✅ 实机复验通过：试动作满幅度、不再撞限位（用户实测 2026-09-18） |
| 实机完整曲目演奏 | ✅ 实机演奏《两只老虎》节奏正确（用户实测 2026-09-18） |

**实机速度基准**（飞特 STS3032 × 6，`speed=0` 最快档）：

| 配置 | 实测 |
| --- | --- |
| 单根，满行程 | **7.10 ~ 7.15 Hz** |
| 5 根同步，满行程 | **4.97 Hz**（单程 100.7 ms，`lost=0`） |
| 5 根同步，行程 80% | 5.87 Hz |
| 5 根同步，行程 **60%** | **7.18 Hz** ✓ |
| 5 根同步，行程 40% | 9.30 Hz |

> 同步根数越多越慢（同步写要等最慢那根）。**要 6 Hz 就减行程换** —— 完整表与拟合公式见
> [firmware/README.md](firmware/README.md) 与 [docs/design-notes.md](docs/design-notes.md) 风险 1。

已知硬件缺陷记录：早先有 1 个舵机接触不良（20 次 ping 仅 6 次通），重新插好后已排除。

---

## 后续路线

**硬件侧已经全部跑通（2026-09-18）**：

- ~~重做槽位 0 的第 3 步校准~~ —— 已由固件 v2.1.2 的 `snapStandby()` 自动修正，
  实机读回 6 槽静止位全落在松开端、行程 = 完整量程（702/692/504/586/815/840 计数），**不用再采样**
- ~~实机演奏一首完整曲目~~ —— 已由用户实机演奏《两只老虎》验证，节奏正确

**唯一还没定的是 Q9（速度取舍）**：原始需求是「行程 80% + 6 Hz」，但实测 80% 行程只有 5.87 Hz。
是牺牲行程保频率，还是牺牲频率保行程？倾向保频率 —— 见 [docs/design-notes.md](docs/design-notes.md) §2 Q9。
**它不阻塞使用**：默认满行程 4.97 Hz 已过 4 Hz 保底线；想上 6 Hz，把演奏页的「按压幅度」滑块拉到 60% 即可（实测 7.18 Hz）。

完整步骤与原始需求见 **[docs/handoff-2026-09-17.md](docs/handoff-2026-09-17.md)**。
方案决策、待定问题、技术风险、里程碑见 **[docs/design-notes.md](docs/design-notes.md)**。
协议细节、寄存器表、逐条 bug 剖析见 **[firmware/README.md](firmware/README.md)**。

---

## 关联仓库

- [dual-n20-pot-motor-driver](https://github.com/cdh66666/dual-n20-pot-motor-driver) —— Web Serial 实时调参与曲线可视化思路
- [dual-esp32-motor-force-feedback](https://github.com/cdh66666/dual-esp32-motor-force-feedback) —— 舵机同步总线与闭环调试工具链
- [Bus_Servo_Driver](https://github.com/cdh66666/Bus_Servo_Driver) —— 飞特总线舵机驱动板
