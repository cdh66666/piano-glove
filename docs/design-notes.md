# 方案笔记 · Design Notes

> 记录关键技术决策与待定问题。已定的写结论，未定的留在待定区，不要凭猜测填死。
>
> 文档分工：
> - **本文** —— 决策、待定问题、风险、里程碑。**「为什么这么定」写这里。**
> - [firmware/README.md](../firmware/README.md) —— 协议细节、寄存器表、逐条 bug 剖析、实机速度基准。
> - [handoff-2026-09-17.md](handoff-2026-09-17.md) —— 交接当时的原始资料索引。

---

## 1. 已定（Decision Log）

| 日期 | 决策 | 理由 |
| --- | --- | --- |
| 2026-09-17 | 仓库 `piano-glove` 建立，Public | 与既往项目保持一致的开源习惯 |
| 2026-09-17 | 归档交接资料，`host/` 存放 PC 侧工具，与固件不兼容的旧工具移入 `host/legacy/` | legacy 里的手指 SVG 动画、示波器、跟随误差评估仍有 UI 复用价值 |
| 2026-09-17 | **产品形态确认为「舵机驱动型」**——舵机带动手指按压琴键，不是传感器→MIDI 的输入型手套 | 交接资料（6 舵机 / PRESS / RELEASE / DEMO）表明动力流向是板子 → 手指 |
| 2026-09-17 | 沿用主控板自带的自定义 `PIANO_GLOVE_1` 固件，不刷微雪官方固件 | 协议已逆向完成，重刷固件成本高于收益；仅当需要「频率驱动」且上位机方案不达标时再评估 |
| 2026-09-17 | 舵机档位固定 `STS3032` | `SC09` 档下 STS3032 舵机 ping 不通 |
| 2026-09-17 | **上层需求明确：输入带指法的 MIDI 文件驱动手套**，让人戴着手套感受原曲的节奏与速度 | 手套只有 6 个自由度而钢琴有几十个音，因此策略定为「保节奏、允许丢音」，见 §3 风险 7 |
| 2026-09-17 | 调试台重写为**底部导航分页式**（连接/编号/校准/试动作/演奏/日志），单文件零依赖 | 使用者要能照着页面顺序一步步走；SPA 而非多文件是因为串口连接状态必须跨页保持 |
| 2026-09-17 | MIDI 解析**自己实现**（不引第三方库） | 保持单文件可直接双击/localhost 打开；浏览器外网 CDN 不可靠 |
| 2026-09-17 | 按住时长取音符时长的 80%，夹在 55~200ms | 早先取 ~50ms 时手指几乎不动、手套没有「弹」的感觉；这个区间接近真人触键 |
| 2026-09-17 | 编号向导改为**状态机**：必须检测到总线为空才允许插下一个 | 直击 `assign_log.txt` 里「反复刷『请把它拔下来』」的死循环 |
| 2026-09-17 | 建立 `host/tests/`，用模拟固件做无硬件端到端自测 + MIDI 解析器交叉验证 | 用户要求「先确认没问题再接硬件」 |
| 2026-09-18 | `Transport.send()` 从「发完死等 `timeoutMs`」改为「收到第一行后安静 140ms 即返回」，并把往返耗时写进 `T.last` | 原实现根本量不出延迟（返回时刻与回复无关）；改完既能量延迟，又让编号向导这类连续命令快一个数量级 |
| 2026-09-18 | 调试台加「4B 响应速度实测」：`PING` 往返 / `MOVE` 往返 / 连喷 60 条压测 | 这是决定演奏可行速率的唯一硬数字（6 指 × 6 Hz × 2 = 72 条/秒），必须在上实机前就位 |
| 2026-09-18 | 延迟结果用整块卡片显示，不用表格 | 5 列表格在窄屏被切掉；「新手小白能看懂」优先于信息密度 |
| 2026-09-18 | **改为主控板固件重写（自研 `PIANO_GLOVE_2`）**，替掉原厂闭源 `PIANO_GLOVE_1`；原厂 4MB 整片 flash 已备份到 `_inbox/fw_backup/` | 原厂固件每条命令固定约 103 ms 等待窗口，受理上限 9.9 条/秒；需求是 6 指 × 6 Hz × 2 = 72 条/秒，差 7.3 倍。Q1 定案后由用户明确授权「直接重写固件，不需要用别人的固件」 |
| 2026-09-18 | 自研固件走**文本协议逐命令兼容**路线，而不是换二进制协议 | `host/web_piano_glove.html` 与 `host/glove_fw.py` 一行都不用改，风险集中在固件侧且可回滚；换协议要两边同时动 |
| 2026-09-18 | 后台在线状态轮询改**轮转单槽 + 8 ms 超时**（原为一次刷 6 槽 × 20 ms） | 一次刷 6 槽要 120 ms，会把这期间到达的所有命令推迟；演奏场景不允许这种周期性抖动 |
| 2026-09-18 | 串口 RX 环形缓冲从默认 256 B 放大到 **4 KB**（`setRxBufferSize` 必须在 `begin` 前调用） | 256 B 在 115200 下只有 22 ms 余量，上位机一次连发 60 条约 1.1 KB 会溢出丢字节，表现为「发 60 条回 45 条」 |
| 2026-09-18 | **「试动作」点下去只锁电机不动 —— 根因是板子跑的是 v2.0.1 旧固件**，修好的 2.1.0 从未烧进去 | 实测 `INFO` 回 `ver=2.0.1`、`BUSINFO` 无回复。**结论：改完固件必须真烧录并回读 `INFO ver=` 复核**，「本地编译通过」不等于「板子上是这版」 |
| 2026-09-18 | `sendRecv()` 组包缓冲 `pkt[32]` → **`pkt[80]`**，并统一所有接收缓冲为 `RESP_BUF = 96` | `SYNC_WRITE` 一次写 6 根要 `2 + 7*6 + 6 = 50` 字节，`pkt[32]` 从第 4 根起写穿栈 → `Stack smashing protect failure!` 直接复位。RATE 一启动就崩就是这个 |
| 2026-09-18 | **`SETID` 拆成双语义**：`SETID <old> <new> CONFIRM` = 修号（`remapId`）；`SETID <old> <new> CONFIRM <slot>` = 编号向导（`setMap`，只认这一块） | 出厂新舵机 id 全是 1，编号向导第 2 轮发 `SETID 1 2` 按「修号」走会把第 1 轮已填好的槽位 0 一起改成 2，6 轮跑完得到 `[2,2,3,4,5,6]` —— 1 号舵机永远不动，界面却显示「编号完成」 |
| 2026-09-18 | 新增 **`RATE <slots> <speed> <acc> <depth%> [cycles]`** 板载测速命令，一次完整行程 = **最小 → 最大 → 最小**，结束打 `RATE_DONE` | 用户要「最小最大最小这样一个完整的算一次」。行程由板子自己跑，才量得到**真实机械频率**而不是命令频率 |
| 2026-09-18 | 测速**必须等舵机到位再发下一半程**，不等就是在量「我发包有多快」 | 早先版本不等到位，量出的是命令频率，与体感无关 |
| 2026-09-18 | 调试台加 **4C 测速卡片**，支持单独测某根 / 某几根 / 全部（掩码 `0x3F`），行程与速度都可调 | 直击用户诉求「可以单独测某根 某几根或者全部一起测试速度」 |
| 2026-09-18 | **模拟固件必须忠实实现固件语义**，尤其 `reset()` 不得清掉 `onAsync`、不得重置档位与校准 | 踩过两次：① `reset()` 清 `onAsync` → `RATE_DONE` 永远发不出，e2e 6 项 FAIL；② 「重置向导」顺带恢复出厂 → 档位掉回 `SC09`（量程 1023），模拟算出 11.6 Hz 而实机只有 5.0 Hz，**偏差 4 倍**。模拟一套、实机另一套，两类 bug 都测不出来 |
| 2026-09-18 | 演奏样本自带《两只老虎》（`host/samples/two-tigers.mid`，362 B / 32 音符），用于验证指法 | MIDI 生成脚本 `host/tests/make_two_tigers.py`；`check_midi.py` 做结构校验 |
| 2026-09-18 | **静止位必须落在「松开端」，不是量程中点**；抽出 `pressEnd()` / `releaseEnd()` 两个概念 | 用户原话「**怎么幅度这么小，要用我校准的最大幅度来啊**」。`autoFinish()` 原先把 `standby` 设为 `(min+max)/2`，而 `pressPos(s,100) = 按下端`，于是只从**中点**走 —— 按压幅度只剩校准行程的**一半**。`dir` 表示「按下去往哪边走」，不是「哪边数值大」 |
| 2026-09-18 | 新增 `snapStandby()`：开机自动把**存量**校准里明显跑偏（离松开端 > 量程 1/4）的静止位对齐到松开端；另给 `CAL ALIGN` 手动触发 | **老校准不用重做**。只修明显跑偏的，避免动到手工校准里有意留的偏移。实机 6 槽全部修正到位 |
| 2026-09-18 | 新增 `CAL ALIGN` 命令 + 校准页「修正静止位」按钮 | 让用户能一键修存量数据，而不是被迫重做第 3 步采样 |
| 2026-09-18 | **`MOVE <id> <pos>` 加校准区间越界闸**：超出该槽位 `[min,max]` 一律拒收回 `ERR MOVE out_of_cal_range ... add_FORCE_to_override`，显式带 `FORCE` 才放行（`FORCE` 在任意参数位置都认） | 用户原话「**测动作会卡死，转到不该转到的地方卡住**」。`MOVE` 是唯一能直接指定**原始位置**的命令，最易撞限位 |
| 2026-09-18 | **界面不再硬编码任何原始位置**：4B「测动作往返」从 `MOVE 1 200` 改为 `PRESS`/`RELEASE` 往返 | 实机读回校准表证实：`200` 对 **6 块舵机全部**落在区间外（区间分别是 `[2006,2708] [516,1208] [3514,4018] [1075,1661] [2418,3233] [552,1392]`），谁都顶死 |
| 2026-09-18 | 演奏的按压深度不再按 MIDI 力度衰减，改为默认**100% 完整校准行程**；新增「按压幅度」滑块（默认 100%） | 「幅度小」在演奏环节同样成立：原 `d = Math.max(0.35, e.vel)`，力度 0.7 就只按 70%。e2e 深度条从「不足 100%」变成 `100% × 6` |
| 2026-09-18 | **模拟固件的校准采样改为双向**（原来从静止位单向朝按压方向走） | 单向采样时 `min` 恰好等于起点，于是「中点 == 起点」，**静止位的 bug 在模拟里完全看不出来**；真机是「伸直 ↔ 握拳」双向动作，起点在中间。这是本项目第 3 次踩「模拟一套、实机另一套」 |
| 2026-09-18 | 单测**同一文件必须串行改**，禁止对同一文件并行发多个 Edit | 本轮踩了两次：并行 Edit 互相覆盖，`snapStandby()` 里的 `save()`、`FW_VER` 的修改都丢过。丢 `FW_VER` 那次是「bin 里版本串还是旧的」才发现的 |
| 2026-09-18 | 新增诊断脚本 `host/tests/diag_cal.py`：逐槽列出 `min/standby/max/方向/行程/当前位置/偏差/valid`，并把「拿某个原始位置去 `MOVE` 会不会越界」算给你看 | 校准类问题**不许猜**。这个脚本一跑就读出真机事实，也是它先报出「旧硬编码 `MOVE 1 200` 对 6 槽全部越界」 |
| 2026-09-18 | `host/tests/e2e.js` 的 `check()` 加计数器，结尾由脚本**自己打印总项数** | 文档里曾同时流传「114 项」与「125 项」两个互相矛盾的数字，因为总数一直是人肉数出来的。脚本自报后文档数字才有唯一来源（当前 **207 项**；另有 `bridge_check.py` **43 项**） |
| 2026-09-18 | **用户实机复验通过**：试动作满幅度、不再撞限位、整曲演奏节奏正确 | 本轮两个 bug（撞限位 / 幅度小）的用户侧验收结论。据此关闭 M8 卡点与 M10；关键是 `snapStandby()` 让用户**免于重做校准**，没有把一个内部 bug 变成用户的返工 |
| 2026-09-18 | **演奏页加「音乐 ↔ 演奏动作」同轴卷帘**：上半张 = 曲子本身（音高 × 时间），下半张 = **参与演奏的 5 根手指**的动作轨（侧摆整场不动，不给它一行），共用一根播放头；丢音画灰影 | 用户原话「**需要能把音乐和演奏动作结合显示**」。原来的演奏页只有动作、没有声音，也看不出「这个音是哪根手指按的」 |
| 2026-09-18 | 声音用 **Web Audio 现场合成**（三角波基频 + 一点二次谐波，快起 + 指数衰减），不用 `<audio>` 播音频文件 | ① 单文件零依赖，不能引外部音源；② 音符本来就是逐条事件产生的，直接由 `note` 算频率，**声音与动作同源同刻**，不会出现「音频和动作对不齐」这种最难查的错；③ 用户不用另外准备音频文件 |
| 2026-09-18 | 默认**只响手套真的按下去的音**，另给「连被丢掉的音也一起响」开关做对比 | 默认要保证「听到的 = 看到的 = 手套做的」三者一致；那个开关是给「完整曲子 vs 手套演奏差多少」用的 |
| 2026-09-18 | 卷帘静态层用**离屏 canvas 缓存**，每帧只 blit + 画播放头；跟随模式的窗口起点**吸附到 2 秒网格** | 每帧重画几百个矩形没必要；而跟随模式下窗口若连续移动，静态层会每帧都失效 —— 吸附到网格后只在跨格时重画 |
| 2026-09-19 | 新增 **10 首分级演奏样本**（L1 入门 → L5 挑战，每级两首），生成脚本 `host/tests/make_samples.py`，曲库内嵌进调试台第 5 页 | 用户要「找十首不同级别的钢琴曲，放到演奏测试里」。分级不是按速度，而是按**同时要几根手指**和音乐复杂度 —— 理由见下一条 |
| 2026-09-19 | **演奏速度分级**：曲库每首自带本级建议速度（L1 90% → L5 200%），选中即套用；滑块上限 150% → **250%**，并加「⚡ 拉满」按钮 | 用户原话「**这个演奏速度分级，要越高级的音乐演奏速度越快呀，达到极限**」。滑块旁同时实时算出「当前速度 = 手套上限的几倍、约多少动作发得出去」—— 把「极限」变成**看得见的数字** |
| 2026-09-19 | L5 的建议速度（200%）**故意**超过手套上限（实测满行程 4.97 次按压/秒） | 那一级存在的意义就是压力测试：看固件接不住时会不会卡死。正确表现是**主动丢音、保住节奏**，不是堵住不动。革命练习曲在这个速度下是上限的 8.9 倍，只有约 11% 的动作发得出去 |
| 2026-09-19 | 负载提示**同时给平均和峰值**两个数 | 只给峰值时 `4 条/秒 × 90%` 四舍五入还是 4，L1 与 L2 显示同一个数，用户拖滑块会以为坏了。平均值是浮点的、对速度敏感，负责「看得出在动」；峰值才是判断「跟不跟得上」的依据 |
| 2026-09-19 | 演奏入口**在第一个 `await` 之前**调 `unlockAudio()`；另加页面任意点击的兜底解锁 + 「🔔 试听」按钮 + 常驻音频状态行 | 用户报「**演奏的时候没有音乐**」。诊断脚本实测页面侧完全正常（`state=running`、`currentTime` 在推进、RMS 非零），所以问题在输出环境 —— 但**光说"你环境有问题"是不负责任的**。现在页面能自证在发声，用户按三步自己就能定位 |
| 2026-09-19 | **串口从浏览器搬到 Python 侧**：新增 `host/bridge.py`，用 pyserial 在本地进程里开串口，同时把 `host/` 当静态站点托管；页面只发 HTTP，不再用 `navigator.serial` | 用户原话「**你就不能整个接串口的吗，每次侧边栏浏览器都用不了**」。Web Serial 的硬约束（电脑版 Chrome/Edge + 安全上下文）决定了**侧边栏浏览器、内嵌 WebView、微信内置浏览器一律连不上** —— 这在那类环境里是必然，不是配置问题。搬到 Python 侧之后任何浏览器都能用，且顺带消掉了"每次弹系统选口框" |
| 2026-09-20 | **演奏命令聚合**：`Transport.fire()` 入队，由 `flush()` 按 **40ms 节拍**合并成一批；桥侧 `/api/fire` 收 `{cmds:[…]}` 一次 `write` | 用户报「**有很严重的延迟，演奏的时候声音和动作不同步**」。根因是逐条 `MOVE` 各走一次 HTTP POST，而浏览器对同一 origin 的并发上限只有 6 条 —— 几十条命令排成长队；声音却是 Web Audio 当场同步发出的，于是越到曲子后段动作落后越多。实测桥侧批量写 40 条 **15.3ms** vs 逐条 **100.5ms**（差 6.6 倍，浏览器里差距更大） |
| 2026-09-20 | **物理约束一律写在真实毫秒域**，再乘速度倍率 `k` 换回曲谱时间（`buildPlan(notes, mode, speed)`） | 用户报「**最快的革命练习曲每个键都是抖动一下，根本没按下去**」。诊断脚本量出：200% 速度下按住时长只剩 **30ms** 真实时间，而同手指相邻两次按下只隔 **94ms**，而舵机走完一次行程要 ~90ms —— 还没按到底就被叫回来了。根因是按住时长按**曲谱**毫秒算（`clamp(n.d*0.8, 55, 200)`），同一个数字换个速度物理含义就完全不同。改完实测：按住 90ms、同手指间隔 188ms、按不到底的音 **0 个** |
| 2026-09-20 | 槽位占用记 `(holdReal + stroke) × k` —— **松开也要时间** | 原来只按 `hold` 算占用，等于默认"松开是瞬移"：下一个音紧接着又来，舵机按到一半就被反向拉走，连击糊成一团。`stroke` 由界面上的**「手指行程时间」**（默认 90ms，来自实机 4.97Hz 满行程的一半）给出，是个真旋钮 |
| 2026-09-20 | 速度变化**必须重算 plan**：`#speed` 的 `onchange` → `analyze()`；拖动中只更新文字 | plan 里的物理约束都按真实毫秒算，速度换了不重算，「按到底」的判断就还停在旧速度上（这正是抖动 bug 会一直存在的原因）。但重算要遍历整曲，跟着 `input` 每帧跑会卡，所以拆成「拖动只改字、松手才重算」 |
| 2026-09-20 | 幅度映射抽成 `ampOf(vel) = 0.45 + 0.55×vel`，**默认跟随 MIDI 力度** | 用户原话「**幅度也是**」。原来的 `max(0.35, vel)` 让弱音几乎不动、看着像没反应；新映射底 45% 保证弱音也看得见，上限 1.0 保证强音按到底（用户要的"用我校准的最大幅度"）。抽成函数是为了让自测能直接断言两端，而不是靠采样 DOM 猜 |
| 2026-09-20 | 声音时长传 `e.holdReal`（真实毫秒）而不是 `e.durMs`（曲谱毫秒）；衰减尾巴 0.30s → 0.20s、最短 0.14s → 0.075s | 用户原话「**音乐也要对的上**」。200% 速度下传曲谱时长会让每个音拖成两倍长，而音符间隔本来就不足 140ms —— 全糊在一起，节奏在但听不清在弹哪个音 |
| 2026-09-20 | 丢音数**写在界面上**（`#dropStat`），「补齐按不了的音」默认打开 | 用户说「**我点开了那个按钮还是没啥变化**」。真实答案是：那首曲子一个音都没丢，开关当然没变化 —— 而界面上原来完全看不出丢了几个（L1~L3 本来就不丢）。现在把数字摆出来，用户一眼知道这个开关有没有用；默认打开是因为丢音是物理必然，补齐才听得到完整曲子 |
| 2026-09-20 | **拇指两轴定案**：`slot0 = 按压`（真把拇指压到琴键上，参与演奏）、`slot1 = 侧摆`（只把拇指摆到位，整场不动）；出厂贴的「拇指侧压 / 拇指下压」标签是**反的**。演奏能力口径从「6 个自由度」改为 **5 根参与演奏的手指** | 用户原话「**钢琴手套里的按下关节才是控制侧摆的，侧摆才是按下的**」。原先按舵机数量算"6 个自由度"，把侧摆那根也派去按压琴键 —— 那不是演奏，是假动作。`PLAY_SLOTS` 成为 `buildPlan()` 唯一的候选集合，分母从字面量 `6` 改成 `ps.length` |
| 2026-09-20 | 新增调试台第 **3C** 步「设定拇指侧摆的使能位」：滑块试位 → 记住（`localStorage` 存 `槽位:位置`）→ 演奏开始自动到位、暂停或正常结束回松开位。侧摆**只在开始 / 结束两个时刻**被碰，急停不管（断电拇指自己就松） | 用户原话「**侧摆的只是使能到合适的位置，这个位置需要校准获得**」。使能位取决于手形 / 连杆 / 拇指起始姿势，**没有公式，只能试出来**。存值**必须绑槽位**：对调角色后，旧值会落在量程完全不同的另一根轴上 |
| 2026-09-20 | 新增「两根轴对调」按钮：只交换 `ROLES[0] / ROLES[1]`，**不交换舵机 ID、不作废校准** | 标签可能是反的，与其让用户重做一遍编号和校准，不如只换角色。校准按**槽位**存、与角色无关，所以换角色天然不该影响它 |
| 2026-09-20 | **音画对齐**：声音改由**独立游标**推进，提前 `baseLatency + outputLatency + 10ms` 提交；演奏页给出「音画对齐」微调（±60ms，存浏览器） | 用户原话「**舵机动作按下去感觉要过一会声音才会响，没有那种声音是从手指按下触发的感觉**」。量出来是**固定**延迟而非漂移：WebAudio 从 `ctx.currentTime` 到真正出声还要过 `baseLatency`(10ms) + `outputLatency`(40ms)，再加 `tone()` 自带的 10ms 调度提前量，共 **60ms**；舵机命令几乎不等，于是听觉整体落在视觉后面。补偿方向是让声音**早提交**、不是晚出声，所以音不会被往后拖。自测必须在**密集曲**上逐音比对提交时刻 —— 稀疏曲（小星星间隔 3.3 秒）上"数音符个数"怎么写都是绿的 |
| 2026-09-20 | **起音点**：出声时刻从「手指一动」改成「手指走到**行程的一半**」。新增「起音点」滑块（0~90%，默认 50%，存浏览器）；卷帘图动作条上画一道**起音刻度**。算式钉死为 `提交时刻 = 按下时刻 + 行程×起音点 − 声卡延迟` | 用户原话「**应该在按下行程的一半就发出声音，这跟弹钢琴也是一样的啊**」。真钢琴是压到中段击弦，不是等琴键按到底；手套同理，手指刚离开待命位（0%）时根本还没碰到琴键，声音搁在那儿会显得抢在手指前头。注意这是**上面那条音画对齐的目标变了**：补偿不再是"把声音拉回按下瞬间"，而是"让声音落在行程一半"——两者相抵后默认净提前 **15ms**（60 − 45）。**卷帘图那道刻度**是因为"声音在动作的哪一刻响"口说无凭：条宽 = 这一按的真实时长，刻度位置 = `行程×起音点 ÷ 按住时长`，长音上贴左、短音上居中，一眼看得出 |
| 2026-09-20 | **演奏收尾：全部舵机失能**（`DISARM` = `armed=0` + `torqueAll(false)`）。演奏页新增「奏完失能」勾选框（默认**开**，存浏览器）；自然奏完 / 手动停都走这条路；**先**把拇指侧摆送回松开位、**后**断电；急停（■ 停止）仍走 `SAFE`；本地记 `S.needArm`（**两条断电的路都记**：`finishPlay()` 的 `DISARM` 与急停的 `SAFE`），下次演奏自动重新 `ARM`；另有 `pagehide` 兜底（**演奏中**关页面补发一条 `DISARM`，`keepalive` fetch） | 用户原话「**还有演奏完之后就要全部舵机失能**」。演奏完了手指还带电按在琴键上，舵机一直出力顶着、发烫、耗电、互相较劲；更要紧的是演奏一结束人就要上手摘手套，**带电的舵机被人手硬掰会顶伤齿轮**。两处细节都是踩出来的：① 顺序必须**先回位后断电** —— 固件里 `DISARM` 之后 `MOVE` 一律回 `motion_not_armed`，倒过来的话那条回位命令白跑，拇指停在"对准琴键"的使能位上；② 断电后光看 `S.info.armed` 会**以为还使能着**（它是发 `INFO` 查回来的，断电到下次查询之间是旧值），于是刚发的 `MOVE` 全被回 `motion_not_armed`、手指一动不动 —— 所以加本地标记 `S.needArm`，并在 `refreshInfo()` 里以板子回报为准撤掉它 |
| 2026-09-20 | **页面按候选清单朝绝对地址找桥**（`?bridge=` → 本页来源 → `127.0.0.1`/`localhost` 的 `8123~8130`）；桥侧回 **CORS 头**（镜像 Origin，不是 `*`）并实现 `do_OPTIONS` 预检；`_origin_ok()` 显式放行 `null`（`file://`） | 用户截图报「**没找到本地串口服务**」，可桥明明在跑、COM3 也连着。根因是页面用相对路径 `fetch("/api/status")` —— **只能同源用**：被别的本地服务器托管时问到别人家、404；`file://` 双击打开时更彻底，相对路径被解析成 `file:///C:/api/status`。**自测从没照过它**：第 8 节的桥测试一直用桥自己的地址同源打开，把这条路径焊死在「正确用法」上。现在新增跨来源 + `file://` 两页断言 |
| 2026-09-20 | **硬拉黑的口不再兜底连接**：`self.ports[:1]`（"全拉黑了也试一个"）改为只试 `score > 0` 的口 | 发现桥连着 **COM1**（`ACPI\PNP0501`，主板自带通信口，score −1000）。原来是"别彻底放弃"的兜底逻辑把它当成手套打开了（打开它不报错），**之后桥再也不去找真板子**：用户插上板子，界面显示"已连接 COM1"，然后什么都动不了 —— 比"没找到设备"难查一百倍。拉黑就该是真的拉黑 |
| 2026-09-20 | 演奏期间**不逐条写日志**，停止后补一条「N 条命令 → 合并成 M 批（单批最多 K 条）」的摘要 | 整曲 480 条命令就是 480 次 DOM 追加，主线程被日志拖住 → rAF 掉帧 → 动作看着更抖。补摘要是因为"批量到底有没有生效"必须看得见，不能只靠测试说 |
| 2026-09-19 | **删掉界面上的「模拟模式」入口**；假固件只保留在 `?sim=1` 后面，作为自测后门 | 用户原话「**我不需要模拟模式，自动选择推荐串口自动连接**」。原来第 1 页第一步就让人二选一，等于把"没硬件也能玩"这个开发期便利强加给用户。自测需要它（上百条断言靠它跑），所以留 URL 开关而不是删代码；第 8 节反过来断言「不带 `?sim=1` 时界面上没有模拟模式」 |
| 2026-09-19 | **「自动选推荐串口」= 按芯片 VID 打分 + 逐个打开问 `INFO`，谁答连谁**；蓝牙虚拟口 / 主板 ACPI 口 / 并口直接拉黑 | 用户要的是"不用选"。打分表：CP210x 100（微雪板载）> CH340 92 > ESP32-S3 原生 USB 88 > FTDI 84 > 认不出的 USB 口 20。拉黑不是洁癖：**开串口会让 ESP32 复位**，拿一次开关换一次"它是不是板子"的答案，不该浪费在蓝牙口上 |
| 2026-09-19 | 自动连接改成「**直接开、开完耐心问 4 次 `INFO`**」，不再是「先探测、通了再连」 | 探测和连接都要 open，open 就复位 —— 先探后连等于复位两次，而且探测那一发 `INFO` 常常打在启动过程里，把真板子判成"不是它"。这条是自测逼出来的：它要求"推荐口不对时会继续试下一个，直到找到板子"，而旧逻辑在第一个口上就栽了 |
| 2026-09-19 | `/api/disconnect` **同时置 `paused`**，自动重连循环让路；`/api/connect` 再清掉 | 不这么做的话「断开」按钮是假的：前脚断开、后脚 1 秒一轮的重连循环又给连回来。变异测试专门覆盖了这条 |
| 2026-09-19 | 桥的 `Bridge.send()` **把「命令发出前就在缓冲里的行」单独作为 `pre` 还给页面** | 板子上电会主动上报（`BOOT` 之类）。不分出来的话它会混进命令回复里 —— 编号向导那种按行解析的流程会被一行无关输出带偏。相应地 `open()` 里**不许** `reset_input_buffer()`（那会把 `BOOT` 直接吃掉） |
| 2026-09-19 | 桥只 bind `127.0.0.1`，并**校验 `Origin`**，跨站请求一律 403 | 不然随手打开的某个网页就能 `POST /api/fire` 去拧用户的舵机。这不是防黑客，是防"网页"这个媒介本身太容易误触 |
| 2026-09-19 | 桥的测试用 `tests/fake_glove.py`（TCP 说手套协议）配 pyserial 的 `socket://`，**只假"对面那块板子"** | 这样 bridge、HTTP、pyserial、收发线程、长轮询全是真的。`/api/lines` 能不能捞到 0.45 秒后才吐的 `RATE_DONE`，只有这种结构才测得出来 |
| 2026-09-19 | 音频断言从「读 `AudioContext.state`」改为**包一层数调用次数** | headless Chromium 里 `AudioContext` 一建出来就是 `running`，读 state 永远为真 —— **把 `unlockAudio()` 整行删掉也测不出来**（假绿）。变异测试证实：删掉调用后新断言报 `n:0`，而 state 仍是 `running` |
| 2026-09-19 | 速度分级的常量（`LEVEL_SPEED` / `GLOVE_MAX_PRESS_HZ`）**必须声明在 `SONG_LIB` 之前** | 曾经放在文件后半段，顶层初始化一句 `applyLevelSpeed()` 就撞 TDZ 抛错，**整个顶层脚本从那行断掉**（连 `#songLib.onchange` 都没绑上）。用户看到的是「曲库下拉换了没反应」，e2e 报的是「两条曲库断言 FAIL + 页面导航错误」—— 极难定位 |
| 2026-09-19 | 「越高级越快」在报告里**如实按倍率说**，并单独解释绝对频率还受谱子密度影响 | 卡农每音两拍、24 个音铺满 35 秒，给 L4 的 170% 也只有 1.2 次/秒，比 L1 的《小星星》还慢。与其让那张表看起来"矛盾"，不如把原因写清楚 —— 数据要能对账，不能只挑好看的说 |
| 2026-09-19 | **样本里绝不写指法**：MIDI 只含「曲名/速度/拍号/音符」四类事件，手指分配一律由 `buildPlan()` 导入时现算 | 指法要和**这一副手套**绑定（哪根手指是哪个槽位、校准到哪、方向如何），这些不在曲子里。写进 MIDI 的话，换一副手套就得把样本全部重做。自检是机器做的：扫全部 meta 类型，只放行 0x03/0x51/0x58/0x2F，出现 `Text`/歌词直接报错 |
| 2026-09-19 | 指法表 `samples/FINGERING.md` 由 `fingering_report.js` **驱动真实浏览器**跑页面里那一份 `buildPlan()` 导出，不另写一套算法 | 若为了出报告另实现一遍分配算法，报告写一套、演奏执行另一套，两边迟早对不上而且没人会发现。多花几秒起个浏览器的成本，换「报告里的指法」与「手套执行的指法」在定义上就是同一份数据 |
| 2026-09-19 | **修：音高对应模式的音域分配** —— 去掉 `hi` 的兜底 72，`want` 从 `floor(ratio*6)` 改为 `round(ratio*5)` | 《小星星》的 C D E F G A 本来该一根手指一个，实际只占满 **4 根**（E/F 挤在食指、小指全程闲着）。两个错叠加：兜底把 9 个半音的音域撑到 12；`floor(*6)` 让最高音算成 6、被 `min(5,…)` 压回 5 又和倒数第二个音撞车。**这是被指法报告当场照出来的**，否则会一直以为「音高对应 = 一个音一根手指」 |
| 2026-09-19 | 实测结论：**「音符密」压不垮手套，决定成败的是同一时刻要几根手指** | 原本 L5 准备的是一首极快的十六分单音跑动，结果**零丢音** —— 触键时长夹在 55~200ms，6 根手指轮转绰绰有余，单声部再快也只占一根手指。于是 L5 换成《革命练习曲》：左手十六分琶音 + 右手每拍柱式和弦，拍点同时响 5 个音。可比的指标是**峰值命令速率**：L1 是 4 条/秒，L5 是 44 条/秒 |
| 2026-09-19 | 生成器对**同一时刻的重复音高去重** | 编配多声部时左手琶音的音会落在右手和弦的音上（都是 Cm 的组成音）。不去重的话文件标称 256 个音、解析出来只有 240 —— 一个键按不了两次，解析器会把后一个 note-on 当重复触发合并掉 |

| 2026-09-20 | **拇指两轴定角色**：`slot0 = 按压轴`（真把拇指压到琴键上，**参与演奏**）、`slot1 = 侧摆轴`（只把拇指摆到位，**不参与演奏**） | 用户实机确认「按下关节才是控制侧摆的、侧摆才是按下的」——出厂贴的标签反了。见下面 §3.5 |
| 2026-09-20 | 两轴角色做成**页面上一键对调**，且只换角色、**不换舵机 ID、不动校准** | 谁是谁取决于装配时舵机臂的方向，不该逼用户去改固件；而交换 ID 会让 A 槽位的校准数据发给 B 舵机（比"不演奏"更难查）。校准是按槽位存的，角色与它无关，所以对调不废校准 |
| 2026-09-20 | **侧摆的「使能位」由用户试出来并存在页面（`localStorage`），不加固件命令** | 使能位取决于手形 / 连杆 / 拇指起始姿势，算不出来；而它只是"演奏前发一条 `MOVE`"，用现成命令就够了 —— 固件不必为它改一行。存成 `槽位:位置`，对调角色后旧值自动作废 |
| 2026-09-20 | 演奏能力从「6 个自由度」改为「**5 根参与演奏的手指**」，`buildPlan` 的候选集合 = `PLAY_SLOTS` | 侧摆整场不动，把它算成能按琴键的手指是假的（"同时 6 个音"不成立），而且它会被派去按压琴键 |

> **航道修正**：本仓库初建时曾按「传感器 → MIDI 输出的演奏型手套」规划 README，与交接资料不符，已按实际的**舵机驱动型**重写。
> 注意方向别搞混：手套是**被 MIDI 驱动去动手指**，不是把手指动作转成 MIDI。
>
> **自由度口径修正（2026-09-20）**：早期文档写的「6 个自由度」按舵机数量算；
> 按"能独立按琴键"算其实只有 **5**（拇指那两根轴里只有一根真的按下）。
> 凡是拿"6"当"能同时按几个音"用的地方都已经改成 5。
> 协议原文用的就是「六个主动自由度」——那是硬件口径，和"能同时按几个音"不是一回事。
>
> **命令速率口径修正（2026-09-20）**：文档里到处写的 **72 条/秒**是**内部工程目标**
> （6 轴 × 6 Hz × 2 条命令，按全部 6 根留余量推的），**协议原文里没有这个数**。
> 协议写的硬指标是「**样机实测最高可用频率不低于 2 Hz**」（某一根手指从初始位置下压再抬回
> 记一个完整周期，见协议二·4）。按 5 根演奏手指折算：
> 协议底线 5 × 2 × 2 = **20 条/秒**，快曲目标 5 × 6 × 2 = **60 条/秒**。
> 实测固件稳态 1421 条/秒，三条口径都远远满足 —— 但引用时别再把 72 说成"协议要求"。

---

## 2. 待定问题

| # | 问题 | 影响面 |
| --- | --- | --- |
| ~~Q1~~ | ~~**0~6 Hz 正弦往复驱动怎么实现？**~~ **2026-09-18 已定案：必须改固件**。实测固件受理上限 9.9 条/秒，需求 72 条/秒，上位机路线差 7.3 倍。见 §3 风险 1 | ✅ 已定案 |
| ~~Q2~~ | ~~slot0 / slot1 到底是「拇指侧压」还是「拇指下压」？~~ **2026-09-20 已定案**：标签是反的 —— **slot0 = 真按下、slot1 = 侧摆摆位**；且**侧摆不参与演奏**。见 §4「拇指两轴」 | ✅ 已定案 |
| Q3 | **音符 → 6 个槽位的分配策略哪种体感更好？** 现提供「轮流分配」和「音高对应」两种，需实机对比 | 决定演奏页的默认选项 |
| ~~Q4~~ | ~~接触不良的那个舵机：换线还是重压端子？~~ **2026-09-18 已解决**：6 块舵机 `id=1..6` 全部在线，`SCAN` 与 `MOVE` 逐根实测均可驱动 | ✅ 已解决 |
| Q5 | 手套的机械结构：连杆还是绳驱？行程 80% 对应的角度是多少？ | 影响 `CAL` 的 min/standby/max 取值与按压力度 |
| Q6 | 「带指法的 MIDI」具体是什么格式？ | 标准 MIDI 不携带指法信息。若你们的数据用特定 track/channel/控制器编码了指法，需要针对性解析；当前按「轨道/通道/音域筛选 + 自动分配」过渡 |
| Q7 | 板子自带 WiFi AP（`PIANO_GLOVE_A50528` / 192.168.4.1）的功能要不要探索？ | 若能走 WiFi 控制，可摆脱 USB 线缆 |
| Q8 | 仓库 License | 当前无 |
| **Q10** | **串口桥要不要打包成单文件 exe？** 现在要先有 Python（`start-debug.bat` 会在缺 pyserial 时自动装），打包后就是"双击一个 exe"。 | 只影响分发便利性，不影响功能。倾向打包：用户是把它当工具用的，不该要求他先配 Python 环境 |
| **Q9** | **行程与频率怎么折中？** 原始需求是「行程 80% + 6 Hz」，但 5 根同步实测：80% 只有 5.87 Hz，60% 才有 7.18 Hz。是**牺牲行程保频率**，还是**牺牲频率保行程**？ | 直接决定演奏页默认行程。倾向「保频率」——因为需求里「感受节奏和速度」是主目标，而按压行程差 20% 的按键力度差异，手指的体感分辨不出来。<br>**当前落地（2026-09-18）**：默认 100% 满行程（4.97 Hz，已过 4 Hz 保底线），演奏页新增「按压幅度」滑块，随时可拉到 60%（7.18 Hz）。用户实机复验后未要求改默认值，**该问题不阻塞使用** |

---

## 3. 关键技术风险

**1. 实时驱动的吞吐瓶颈（首要风险）—— 2026-09-18 已实测，结论：必须改固件**

原始需求要求「按行程 80%、0~6 Hz 正弦往复」。当前固件**没有频率驱动命令**，只有 `MOVE <id> <pos> <speed> <acc> ARM` 这种一次性定位。

**实测数据**（`host/bench.py`，COM3 / `PIANO_GLOVE_1`，6 个舵机全不在线）：

| 项目 | 实测值 |
| --- | --- |
| 单条 `INFO` / `CAL STATUS` 首字节往返 | **约 62 ms** |
| 单条 `PING 1` 首字节往返 | 约 123 ms |
| 单条 `STATUS_ALL` 首字节往返 | 约 680 ms（要轮询整条总线） |
| 上位机侧写入 60 条命令 | 1.4 ms（≈ 44000 条/秒，**这一侧不是瓶颈**） |
| **固件受理速率（连发 60 条实测）** | **≈ 9.9 条/秒**（每条约 103 ms） |
| 压测后是否仍响应 | 是（63 ms 恢复），不崩、不丢包 |

**结论**：需求 72 条/秒（6 指 × 6 Hz × 按下+抬起），实测天花板 **9.9 条/秒，差 7.3 倍**。

- **(a) 上位机逼近 —— 走不通。** 108ms / 103ms 不是舵机响应慢，是固件**每条命令都有固定的受理窗口**。串口 8KB 缓冲能吞下突发，但固件吐回复的速度就是 ~10 条/秒。演奏页「不等回复」策略不会卡死，但节奏会一路往后拖，几分钟就偏出好几秒。
- **(b) 改固件 —— 唯一可行路径。** 新增按频率往复驱动的原生命令，让板子自己跑正弦，把定时交给板子。**但固件源码不在本机**，等于要重写并重刷。成本高，但这是唯一能满足 6 Hz 的路。
- 注意 62~123ms 是**乐观下限**：舵机还没在线，在线后 `PING` 还要等舵机回包，会更慢。

> 复跑：`python host/bench.py`（自动找板子）／调试台第 4B 页（图形界面）。

**2026-09-18 收尾：风险已解除。** 自研固件 `PIANO_GLOVE_2` 上线后，突发 60/60 ≈ **413 条/秒**、
稳态 60/60 ≈ **1421 条/秒**，比原厂 9.9 条/秒快两个数量级，72 条/秒的需求留了 5.7 倍余量（稳态口径 20 倍）。

真正的新瓶颈**不在通信，在机械**：舵机本身的往返时间才是天花板。实测（`speed=0` 最快档）：

| 配置 | 实测频率 |
| --- | --- |
| 单根，满行程 | **7.10 ~ 7.15 Hz**（单程 70 ms） |
| 4 根同步，满行程 | 5.05 Hz |
| 5 根同步，满行程 | **4.97 Hz**（单程 100.7 ms，`lost=0`） |
| 5 根同步，行程 80% | 5.87 Hz |
| 5 根同步，行程 **60%** | **7.18 Hz** ✓ |
| 5 根同步，行程 50% | 8.07 Hz |
| 5 根同步，行程 40% | 9.30 Hz |

拟合 `半程耗时 ≈ 22 ms + 行程计数 / 6.6`（22 ms 是固件与总线固定开销，与行程无关）。
**这张表和 [firmware/README.md](../firmware/README.md#实机速度基准飞特-sts3032--6speed0-最快档) 里的那张是同一份数据，
改一处必须同步另一处；重测命令见文末「复跑入口」。**

**结论**：用户「6 Hz，最不济 4 Hz」的指标 —— **满行程 4.97 Hz 已保底达标；行程压到 60% 即稳定过 6 Hz**。
同步根数越多越慢（同步写要等最慢那根），所以「6 指全开 + 满行程」是 5 Hz 档，需要 6 Hz 时靠减行程换。

**2. 固件源码缺失**

`PIANO_GLOVE_1` 是别人写的闭源固件（交接前的前置工作已逆向其协议）。风险 1 的实测已经把这条路堵死了 —— **改固件不再是「可选优化」，而是继续推进的前置条件**。首先要解决的是「源码在哪 / 要不要重写」。目前 `firmware/` 里只有说明，没有源码。

**3. 舵机编号流程极其容易做废**

6 个舵机出厂 ID 全是 1，同总线会互相抢答，`SETID` 可能同时命中多个。必须一次只接一个。`assign_log.txt` 里已记录一次失败：编号向导在第 2 轮反复报「总线上还有 [1] —— 请把它拔下来」，说明**流程对人工插拔时序敏感**，需要更明确的提示或自动判别。

**2026-09-18 又挖出第二个、更隐蔽的坑**：出厂新舵机 id 全是 1，第 2 轮插上来的那块**也是 1**。
此时发 `SETID 1 2`，若按「修号」语义走 `remapId(1, 2)`，就会把第 1 轮已经填好的槽位 0
（它记录的 id 也是 1）**一起**改成 2。6 轮跑完得到 `[2,2,3,4,5,6]`：

| 轮次 | 操作 | 期望映射 | 实际映射 |
| --- | --- | --- | --- |
| 1 | `SETID 1 2` slot=0 | `[2,-,-,-,-,-]` | `[2,-,-,-,-,-]` |
| 2 | `SETID 1 3` slot=1，remap 命中槽位 0 | `[2,3,-,-,-,-]` | `[3,3,-,-,-,-]` ✗ |
| … | … | … | … |
| 6 | 全部完成 | `[1..6]` 顺序打乱 | `[2,2,3,4,5,6]` ✗ |

后果是**槽位 0 和槽位 1 指向同一块舵机，1 号那块永远不动**，而界面一路显示「编号全部完成」。

修法（已落地，固件 v2.1.1）：
1. `SETID` 带 `slot` 参数时走 `setMap(slot, new)` —— **只认这一块，别的槽位一律不碰**；
2. `ENROLL` 状态机发现 `cur == 目标 id` 时也必须**补写映射**（原来直接 return，映射就丢了）；
3. 上位机编号流程在 `cur === target` 时补发 `MAP <slot> <id>`。

**回归防线**：`host/tests/e2e.js` 不再断言界面文案，而是直接读固件映射 ——
`STATUS_ALL` 解析出的槽位 id 序列必须等于 `1,2,3,4,5,6`。
做过变异测试：把修复退掉，模拟固件给出 `[2,2,3,4,5,6]`，该断言立刻变红（`失败 2 项 ✘`）。

**4. 多轴协同的一致性**

6 个舵机按压同一只手，如果各轴 `CAL` 的 standby/min/max 不一致，「同时按下」会变成参差不齐的错音。校准必须逐槽位验证。

**5. `SCAN` 漏报与 `PING` 慢**

`SCAN` 约 1/3 概率返回 `count=0`；`PING` 单次约 123 ms（舵机不在线时），遍历 10 个 ID 仍要 1.3 秒。导致任何「上线检测」都慢且不稳，上位机轮询建议 2~3 秒一次。

注：早先记录的「`PING` 单次 610 ms」是**含读取尾部静默超时的合计值**，2026-09-18 用首字节计时修正为 ~123 ms；但**固件受理速率仍是硬伤（见风险 1）**。

**2026-09-18 随固件重写解除**：自研固件把舵机侧超时窗口收到 20 ms，`SCAN` 不再出现 `count=0`；
后台在线状态轮询改成**轮转单槽 + 8 ms 超时**（原来一次刷 6 槽 × 20 ms = 120 ms，
会把这期间到达的所有命令往后推，演奏场景不允许这种周期性抖动）。实机 `SCAN` 稳定报 6 块。

**6. `ENROLL` 流程语义未摸清**

固件自带的舵机注册流程（`ENROLL START/STATUS/STOP/RESET`），其 `requirement` 字段提示了一个三段式流程（先 1→10，再以 ID1 作为 2~6 加入，再 10→1），但语义未验证。**用完记得 `ENROLL STOP`**，否则可能影响正常动作。

**2026-09-18 处置**：原厂那套三段式 `requirement` 语义**不沿用**，自研固件把 `ENROLL` 重写成
一个直白状态机 —— **必须检测到总线为空（只有本机）才允许插下一块**，插上后自动判定 id、
必要时改号、然后写映射，全程打 `ENROLL ASSIGNED id=N slot=M`，人只要照着提示插拔即可。
这条正是为了解决上面 `assign_log.txt` 里「反复刷『请把它拔下来』」的死循环。

**7. MIDI 演奏的实时性（新）**

一个音符要发 2 条命令（按下 + 松开）。实测样本曲目在 6 秒内有 85 个音符 → 峰值约 **58 条命令/秒**，平均 27 条/秒。

- 模拟模式下完全无压力，但模拟固件是**零延迟**的，不代表实机。
- ~~实机已实测：固件受理上限 ≈ 9.9 条/秒，峰值 58 条/秒根本发不完 —— 差 5.9 倍。~~
  **2026-09-18 已随固件重写解除**：自研固件稳态 ≈ 1421 条/秒，58 条/秒连 4% 都不到。
- 应对（已实现，保留）：播放引擎每帧最多补发 40 条命令，超出部分**直接跳过** —— 宁可丢音，也不让延迟累积把后面的节奏整体推歪。
  新固件下这个上限不会再被触发，但作为兜底留着。
- **新的真实瓶颈是机械而非通信**：一个音符要 2 条命令（按下 + 松开），而一次按压的物理行程耗时
  （满行程约 100 ms，见风险 1 速度表）**远大于**发命令的时间。所以演奏能多快，取决于
  「按住时长 + 抬起时间」而不是总线带宽 —— 这也正是为什么按住时长要夹在 55~200 ms（见 §1）。

**8. 5 根参与演奏的手指 vs 钢琴的十几个音**

手套有 6 个执行器，但拇指那两根轴里只有一根真的按下琴键（见 §3.5），
**能同时按几个音的天花板是 5**。出现「同时需要 6 个音」时必然丢音。当前策略是按空闲槽位贪心分配，实在没有空闲就丢。**对「感受节奏和速度」这个目标来说，保住时间轴比保住每个音更重要** —— 所以丢音是设计选择，不是 bug。

**9. 校准是按「槽位」存的，不是按舵机 ID 存的（已踩实机）**

`CAL` 记录的是**槽位 → min/standby/max 计数**，槽位背后是哪块舵机由 `MAP` 决定。
所以 **一旦动过 `MAP` / `SETID`，旧校准就作废**，必须重做第 3 步校准。

实机已踩到一次：**槽位 0 的校准数据实际是拿 2 号舵机采的**（当时映射还是 `[2,2,3,4,5,6]`）。
映射修成 `[1,2,3,4,5,6]` 后，槽位 0 指向 1 号舵机，而记录里的「静止位 782」
与 1 号舵机的实际位置 **2091** 差了 **1309 计数（≈115°）** —— 照这个校准去按，
会直接顶到机械限位。

- 处置：已用 `MAP 0 1` 修正映射。**槽位 0 的旧校准数据不需要重做** —— 真正的原因不是
  「校准采错舵机」，而是静止位取错了端点（`autoFinish()` 用了量程中点），
  固件 v2.1.2 的 `snapStandby()` 一开机就把静止位对齐到松开端（实机读回见文末表格）。
- 防线：`host/tests/bench_move.py` 内置安全闸 —— 校准值与当前位置差超过 **600 计数**
  就判定「这份校准不是这块舵机的」，**默认跳过该槽位不动**，要测必须显式加 `--force`。
  实测就是靠这个闸先发现异常的。

**10. `INST_WRITE` 的第一个参数必须是起始寄存器地址**

飞特 SCS/STS 的 `INST_WRITE`（指令码 3）参数列表的第一字节**不是数据，是寄存器地址**。
漏掉它会让舵机把数据当成地址，回包错误位非 0 —— **而错误位在第 5 字节，不是指令码**，
所以「舵机回包了」不等于「舵机照做了」。协议细节见
[`../firmware/README.md`](../firmware/README.md) 对应章节。

---

## 3.5 拇指两轴：按压 vs 侧摆（2026-09-20 用户实机定案）

### 事实

| | slot0（舵机 id 1） | slot1（舵机 id 2） | slot2~5 |
| --- | --- | --- | --- |
| 真实动作 | **把拇指压到琴键上** | **把拇指往侧面摆** | 各按一根长指 |
| 角色 | `press` | `latch` | `press` |
| 参与演奏 | ✔ | ✘ | ✔ |
| 演奏期间 | 按每个音 | **发一条命令到位后就停** | 按每个音 |
| 名字 | 拇指按压 | 拇指侧摆 | 食指/中指/无名指/小指 |

出厂时贴的标签是反的（"拇指侧压"贴在了真的按压轴上），**校准数据不受影响** ——
校准是按槽位存的，`min/standby/max` 描述的是那块舵机自己的行程，跟它叫什么无关。

### 三条硬规则（改代码时别破坏）

1. **`PLAY_SLOTS` 是演奏计划的唯一候选集合**，不是字面量 `6`。
   `buildPlan()` 的音高映射分母、`pickFree()`、卷帘图的轨数、深度条、报告统计
   全部走它。往这条路上加东西时，别再出现 `for(s=0;s<6;s++)` 这种写法。
2. **侧摆只在两个时刻被碰**：演奏开始（摆到使能位）、暂停/正常结束（回松开位）。
   急停（`SAFE`）不用管 —— 舵机断电，拇指自己就松了。
3. **使能位必须跟槽位绑定**（存 `"<slot>:<pos>"`）。只存位置的话，
   用户一对调角色，旧位置就会用在量程完全不同的另一根轴上（轻则摆不到位，重则顶死）。

### 为什么让用户自己"试"这个位置

使能位 = 拇指指尖落在琴键正上方、往下压时不打滑也不蹭到邻键的那个侧摆位置。
它取决于手形、连杆长度、拇指起始姿势，**没有公式**。所以调试台第 3C 步给的是
「滑块 + 试这个位置（`MOVE`）+ 读当前位置 + 记住」，而不是一个算出来的默认值。
没设过使能位也能正常演奏，只是拇指可能没有对准。

### 怎么确认哪根是哪根

调试台第 4 步「先确认拇指这两根轴谁是谁」：分别点 1 号 / 2 号的「按一下」，
看拇指是**往下压**还是**往旁边摆**，不对就点「两根轴对调」。对调只改角色。

---

## 4. 里程碑

- [x] M0 协议逆向完成、上位机工具链可用
- [x] M1 单舵机验证通过（PING 20/20）
- [x] M2 调试台重写为分页式 + 内置模拟固件
- [x] M3 MIDI 解析 + 分配 + 演奏引擎（模拟固件下自测通过，61 项检查全绿）
- [x] M4 无硬件端到端自测脚本（`host/tests/`），MIDI 解析器经独立实现交叉验证
- [x] M4.5 **调试台加「4B 响应速度实测」**（`PING`/`MOVE` 往返 + 60 条压测），并把
      `Transport.send()` 从「死等超时」改成「收到回复即返回」。自测 69 项全绿
- [x] M5 **完成 6 舵机编号 1~6** —— 实机映射已修正为 `[1,2,3,4,5,6]`，`SCAN` 稳定报 6 块
- [x] M6 6 槽位全部在线，`STATUS_ALL` 全部 `online=1`
- [x] M7 角色确认（哪个 ID 对哪根手指）—— 映射已实机核对
      （**按压方向仍待体感确认**，见 Q2）
- [x] M8 校准完成并可 `ARM` / `PRESS` / `RELEASE` / `DEMO`
      —— 卡点（槽位 0 旧校准）已由 `snapStandby()` 解除，实机 6 槽 `valid=1`，见风险 9
- [x] M9 **实测固件受理速率**（`host/bench.py`，原厂）：**9.9 条/秒**，需求 72 条/秒 → Q1 定案必须改固件
- [x] M9.5 **自研固件 `PIANO_GLOVE_2` 上线并实机验证**（v2.1.1）：
      稳态吞吐 ≈ **1421 条/秒**（比原厂快两个数量级）；
      `MOVE` 逐根实测 **6 根位移 271~592 计数**、`servo_err` 不涨；
      `RATE` 测速跑通，见风险 1 速度表
- [x] M10.5 **《两只老虎》演奏文件**（`host/samples/two-tigers.mid`）+ 4C 测速卡片
- [x] M10 **实机演奏完整曲目通过**：《两只老虎》，用户实测 2026-09-18（节奏正确、动作不卡）
- [x] M11 **速度指标实测达标**：满行程 **4.97 Hz**（6 Hz 的保底 4 Hz 已过），
      行程压到 **60% → 7.18 Hz**（稳定过 6 Hz）。原始需求「80% 行程 + 6 Hz」需折中，
      因为 80% 行程实测只有 5.87 Hz —— **见风险 1 速度表**
- [x] M13 **音乐 ↔ 动作同轴显示**：Web Audio 现场合成 + 卷帘（音高 × 时间 / 6 条动作轨 / 播放头），
      丢音画灰影；由**同一条事件流**驱动，做到「听到的 = 看到的 = 手套做的」
- [ ] M12（可选）WiFi 控制 / 真正的 MIDI 桥接

### 2026-09-18 硬件接入现状（已打通）

**主板 `COM3`**（CP2102 USB-UART，115200），跑自研固件：

```
OK INFO fw=PIANO_GLOVE_2 ver=2.1.2 profile=STS3032 range=4095
        ap=PIANO_GLOVE_A50528 ip=192.168.4.1
        calibrated=0 armed=0 enroll_active=0 enroll_phase=OFF
        slots=0x3F online=6
```

**6 块舵机（`id=1..6`）全部在线**，`SCAN` 稳定报 6 块。早先「一个都不在线」的记录是
当时 DC 圆口电源/总线没接好，已排除。

> 上面那段是**烧 v2.1.2 之前的快照**（所以 `calibrated=0`）。2026-09-18 晚已烧入 v2.1.2
> 并读回 `INFO ver=2.1.2` 复核，6 槽校准 `valid=1`，用户已实机走完试动作与整曲演奏。

---

### 2026-09-18 用户实机复验结论（已闭环）

用户戴着手套在实机（`COM3` + 6 块 STS3032）上复验后反馈「挺好的」，确认两件事：

- **试动作满幅度** —— 不再只走半个行程，也不再转到限位卡死
- **完整曲目演奏**（《两只老虎》）节奏正确、动作不卡

原本记在这里的用户侧待办「**槽位 0 必须重做第 3 步校准**」**已不需要执行**：
根因不是采样采错了舵机，而是静止位取错了端点，`snapStandby()` 已把它自动修正。
`host/tests/diag_cal.py` 实机读回 6 槽全部 `valid=1`、静止位落在松开端、行程 = 完整量程：

| 槽位 | 名称 | id | min | standby | max | 方向 | 行程 | pos | valid |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | 拇指按压 | 1 | 2006 | 2708 | 2708 | min | 702 | 2359 | ✓ |
| 1 | 拇指侧摆 | 2 | 516 | 1208 | 1208 | min | 692 | 864 | ✓ |
| 2 | 食指 | 3 | 3514 | 4018 | 4018 | min | 504 | 3765 | ✓ |
| 3 | 中指 | 4 | 1075 | 1661 | 1661 | min | 586 | 1368 | ✓ |
| 4 | 无名指 | 5 | 2418 | 3233 | 3233 | min | 815 | 2825 | ✓ |
| 5 | 小指 | 6 | 552 | 1392 | 1392 | min | 840 | 973 | ✓ |

`bench_move.py` 的 600 计数安全闸不再跳过任何槽位。

### 2026-09-18 端到端自测现状

`host/tests/e2e.js` 现在由脚本自己打印总项数：**207 项全绿**（0 FAIL，无 pageerror / console.error）。
此前文档里出现的 114 / 125 两个数字互相矛盾，原因是脚本从来不输出总数、只能靠人肉数 ——
已在 `check()` 里加计数器一并在结尾打印，从此以脚本输出为唯一准数。

### 2026-09-19 串口桥落地（用户报「侧边栏浏览器用不了串口」）

新增两条自测，各自自报总数：

| 脚本 | 项数 | 覆盖 |
| --- | --- | --- |
| `host/tests/bridge_check.py` | **43** | 推荐口打分、自动选口决策顺序、缓存跳过、HTTP API、长轮询异步行、跨站拦截、断开语义 |
| `host/tests/e2e.js` 第 8 节 | 含在 207 里 | **真** bridge.py 进程 + 假手套，验「打开页面不点任何按钮就自动连上」 |

`mutate_bridge.js` 往这条路上注入 3 处退化（改成必须手点、把模拟模式按钮加回界面、
断开后又自动连回来），对应 5 条断言全部变红 —— **确认它们不是假绿**。

`bridge_check.py` 的假东西只有"对面那块板子"：`fake_glove.py` 是个说手套文本协议的
TCP 服务，pyserial 用 `socket://` 就能接上去。真 bridge 进程、真 HTTP、真 pyserial、
真收发线程、真长轮询。

---

### 端口辨认的坑（记录一下，下次别踩）

系统里同时存在一堆 `Status = Unknown` 的幽灵串口（CH340 ×8、CH343 ×4、CP2102 ×2、ESP32-S3 ×5），
是历史上插过的板子留下的注册记录，**不代表设备在线**。
判断在线只看 `Status = OK`，判断是不是手套只能发 `INFO` 看回不回 `OK INFO`。

同时在线但不相关的两个：

| 端口 | 是什么 |
| --- | --- |
| `COM4` | 另一块板子的电机固件（`help` 列出 `motorprofile 775` / `cascade` / `encoder` 等），不是手套 |
| `COM23` | ESP32-S3 原生 USB，被别的进程占着打不开 |

---

### 复跑入口

| 想干什么 | 用哪个 |
| --- | --- |
| 图形界面走全流程 | 双击 `host/start-debug.bat`（起**串口桥** + 打开浏览器）；**别直接双击 .html** —— 那样既没有桥、`file://` 下也拿不到 Web Serial，串口一条都通不了 |
| 只是想手动起桥 | `python host/bridge.py`（自动扫描 + 自动连接），浏览器开 `http://127.0.0.1:8123/web_piano_glove.html` |
| 无硬件端到端自测（207 项） | `node host/tests/e2e.js`（页面带 `?sim=1` 走内置假固件） |
| **串口桥自测（43 项，真进程 + 假板子）** | `python host/tests/bridge_check.py` |
| 确认桥那几条断言会红 | `node host/tests/mutate_bridge.js` |
| **实机验「舵机真的动了」** | `python host/tests/bench_move.py`（自动找板子；比对发指令前后的位置差） |
| 实机测速（可指定某几根） | `bench_move.py --slot 1,2,3`；不带参数测全部 |
| 重生成《两只老虎》 | `python host/tests/make_two_tigers.py`，再 `check_midi.py` 校验 |


